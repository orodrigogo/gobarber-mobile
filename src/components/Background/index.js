import LinearGradient from 'react-native-linear-gradient';
import style from 'styled-components/native';

export default style(LinearGradient).attrs({
  colors: ['#7159c1', '#ab59c1'],
})`
  flex: 1;
`;
